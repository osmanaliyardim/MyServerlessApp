//This is about IConfiguration interface to output data
//for Azure Function (MyFirstServerlessApp)
{
  "version": "2.0",
  "logging": {
    "applicationInsights": {
      "samplingSettings": {
        "isEnabled": true,
        "excludedTypes": "Request"
      }
    }
  }
}